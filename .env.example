# Copy to .env.local for local dev. In Amplify, set this in the
# console under App settings → Environment variables (build-time).
# Pull the value from the SAM `ApiUrl` stack output after deploying infra/.
VITE_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com
